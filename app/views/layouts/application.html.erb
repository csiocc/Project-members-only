<!DOCTYPE html>
<html lang="de">
  <head>
    <meta charset="utf-8">
    <title><%= content_for(:title) || "Members Only" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="color-scheme" content="light dark">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# PWA Manifest bei Bedarf aktivieren %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">


    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="navbar">
      <div class="inner">
        <div class="brand">
          <%= link_to "Members Only", (Rails.application.routes.url_helpers.respond_to?(:root_path) ? root_path : posts_path) %>
        </div>
        <div class="spacer"></div>
        <nav class="main-nav" style="display:flex; gap:12px; align-items:center;">
          <%= link_to "Posts", posts_path %>
          <%# Nur wenn Users-Routen existieren: %>
          <% if Rails.application.routes.url_helpers.respond_to?(:users_path) && user_signed_in? %>
            <%= link_to "Users", users_path %>
          <% end %>

          <% if user_signed_in? %>
            <span>·</span>
            <%= link_to (current_user.try(:name) || "My Account"), edit_user_registration_path %>
            <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "btn btn-ghost" %>
          <% else %>
            <span>·</span>
            <%= link_to "Login", new_user_session_path %>
            <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-primary" %>
          <% end %>
        </nav>
      </div>
    </header>

    <main class="container">
      <% if notice %><div class="flash notice"><%= notice %></div><% end %>
      <% if alert %><div class="flash alert"><%= alert %></div><% end %>

      <%= yield %>
    </main>
  </body>
</html>
